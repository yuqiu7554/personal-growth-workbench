# Contributing

感谢参与个人成长工作台。提交变更前请：

1. 先搜索开放和已关闭 Issue；重大功能在 Issue 中说明用户问题、行为边界和数据影响，确认方向后再投入实现。
2. 不提交密钥、数据库、个人路径、日志、论文全文、真题或音频。
3. 保持本地优先、最小 AI 权限和用户确认原则。
4. 为行为变化补充确定性测试，并运行相关 `scripts/check-*.mjs`。
5. 涉及数据库时提供迁移与回滚；涉及权限时更新隐私文档和 `PrivacyInfo.xcprivacy`。
6. 不复制参考产品的品牌、布局或受版权保护素材。

## 开发流程

1. Fork 仓库，从最新 `main` 创建范围清晰的分支。
2. 保持提交聚焦；界面、数据迁移和外部服务变化不要混在一个无法审查的提交中。
3. 运行相关 `scripts/check-*.mjs`，原生修改还需执行数据库、Bundle 迁移和备份三个自检。
4. Pull Request 说明行为变化、数据与权限影响、验证证据和回滚方式。
5. UI 截图只能使用合成或脱敏数据；失败日志删除本机路径、密钥和用户内容。

维护者可能拒绝偏离本地优先、最小联网、用户确认和版权边界的改动，即使实现本身可以运行。提出大范围变更前先讨论，可避免无效投入。

提交贡献即表示你有权提供该内容，并同意按项目的 `GPL-3.0-only` 许可证发布。涉及第三方代码或素材时必须同时提供来源、版本和兼容许可证证据。
