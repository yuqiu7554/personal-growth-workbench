# 个人成长工作台隐私协议

版本：2026-08-14

## 1. 适用范围

本协议适用于个人成长工作台 macOS 桌面应用及其开源版本。工作台采用本地优先架构，默认不要求注册账户，也不设置开发者运营的用户数据服务器。

## 2. 本地数据

任务、日历、目标、学习记录、论文项目、收藏、健康记录和复盘等核心数据保存在用户设备上的 SQLite 数据库中。用户可以在设置中查看或迁移数据库位置。附件默认采用路径引用；复制、索引或发送附件前必须显示范围并取得确认。

## 3. 凭据

AI API 密钥、有道应用 ID 和应用密钥保存到 macOS 钥匙串。页面只能查询凭据是否存在，不能读回明文。凭据不得写入 SQLite、日志、备份、导出文件、示例数据或开源仓库。

## 4. 联网服务

热点同步按用户启用范围访问政府和监管机构、国际组织、研究机构及权威媒体的公开 RSS、API 或稳定栏目；公众号仅作为人工关注清单，不绕过登录、验证码或平台限制，优先回溯同主体官网。论文同步访问 OpenAlex，并通过 Crossref 交叉核验 DOI。用户启用推荐 AI 加工后，仅把检索获得的题名、来源、日期、DOI 和可用摘要发送给用户自己配置的 AI 服务，用于摘要、翻译、研究关联与阅读优先级，不发送任务、健康、复盘、附件或其他本地记录。

开源版不包含开发者 API 密钥、共享代理或代付账户。每位用户自行配置兼容服务并承担自己的调用费用，不会消耗开发者的 Token。公开来源检索本身不调用 AI；设置页应分别展示可用时由供应商返回的输入/输出 Token、调用次数和估算费用。供应商未返回用量时标记为未知，不推算为精确值。

应用启动时执行漏跑检查，运行期间每15分钟检查一次同步状态。当前版本在应用完全退出后不执行联网任务。用户可以在“设置－推荐同步”中修改关注主题、排除清单或关闭 AI 加工。

## 5. AI 数据边界

AI 调用必须由用户主动触发或明确启用。设置中心提供成长规划、英语学习、IELTS、CET-6、论文项目、今日热点、每周论文、运动健康、今日复盘和周月总结十个独立助手；各助手分别配置服务、模型、开关、调用前预览和预算。发送前应显示模型、用途和数据类别；敏感正文逐项授权。Word、PDF 和图片默认只在本地提取或 OCR，用户校对确认后仅发送文字，不直接上传原文件。AI 生成内容不能静默修改目标、计划、健康记录或成绩。

IELTS 与 CET-6 使用独立助手配置和固定任务边界。IELTS AI 仅处理用户确认的口语转写与写作文本，听力、阅读和原始录音不发送给 AI。CET-6 AI 仅处理翻译与作文文本。未配置经核验的评分规则来源与版本时，不允许生成数字估分或 Band 区间。

论文项目 AI 使用独立的启用状态、服务、模型、预算、项目内对话历史和审计记录。默认只发送当前项目的基本信息、阶段、里程碑、未解决阻塞、未处理导师意见和资料数量。论文正文及附件必须逐项授权，本地提取后仅发送用户确认的文字；原始文件不上传。论文项目 AI 只能提供规划草案，不得直接改写论文正文或静默修改项目数据。

模型供应商返回 Token 用量时，工作台在本地按助手累计。人民币费用只在用户自行填写输入、输出单价后估算；供应商未返回用量或用户未配置单价时明确显示未知，不虚构价格。月度预算达到阈值后暂停相应调用。

## 6. 麦克风、录音与系统转写

IELTS 口语录音使用用户选择或系统默认的麦克风，录音文件保存在用户可配置的本地目录，SQLite 只保存元数据和路径。语音转写仅在用户点击“转写/分析”后调用 macOS 语音识别；优先使用设备端能力，系统必须联网时应提示用户。录音和转写可分别删除，删除后进入30天本地回收站。

## 7. 健康与复盘

健康数据和原始复盘默认仅保存在本机。分享视图隐藏生理期、原始复盘、私人日程和成果文件名。工作台不进行医疗诊断或药物推荐。

## 8. 分析、通知与用户控制

分析和崩溃报告默认关闭，当前版本不发送遥测。通知只有在系统权限和真实调度实现后才能标记为可用。用户可以迁移数据库、删除钥匙串凭据、开启隐私模式或删除模块数据；卸载不会自动删除自选目录中的文件。

用户可在首次启动或设置中关闭一级业务模块。关闭仅隐藏入口并暂停该模块的提醒、同步和自动任务，不删除历史数据或附件；重新启用后恢复访问。永久删除仍必须通过对应模块或隐私中心的独立删除流程完成，模块开关不得被当作删除授权。

资料库删除采用本地回收站：元数据与工作台管理的本地文件保留30天，期间可恢复。应用启动时及运行期间每日检查到期记录；到期后永久删除。永久文件删除仅允许发生在用户曾配置的资料库目录中，不删除外部引用文件。

统一资料库可为 CET-6、IELTS、论文项目和每周论文资料建立本地索引，索引不上传文件，也不自动复制来源模块已经管理的文件。普通上传由用户选择复制到资料库或仅引用原文件；复制文件写入用户选择的本地目录，引用模式只保存路径、指纹和元数据。更改存储位置时，只有用户明确选择迁移才复制并校验受管文件；外部引用文件始终不移动。关闭来源模块不删除其资料索引。

论文项目资料按项目关联。共享资料从单个项目移出时只解除关联；仍被其他项目引用的受管文件不会进入永久删除流程。项目归档停止提醒和 AI 计划，移入项目回收站后保留30天并允许恢复。

## 9. 开源发布

公开仓库只包含源码、空数据库、合成示例和文档，不包含个人记录、密钥、日志、私人路径、版权论文、考试资料、新闻正文或第三方词典批量数据。开源使用者应自行配置合法服务凭据并遵守服务条款。

完整备份使用用户密码通过 AES-256-CBC、PBKDF2 和随机盐加密，包含一致的 SQLite 快照、设置和复制入受管目录的文件。密码不写入数据库、备份或日志。恢复前检查清单并创建当前数据库快照；受管文件采用安全合并，同名现有文件不被覆盖。外部引用文件只保留路径和元数据，不复制其正文。

## 10. 变更

新增联网服务、数据上传、账户、遥测、云同步或商业运营时，必须更新协议版本和变更摘要，并提示用户重新确认。
